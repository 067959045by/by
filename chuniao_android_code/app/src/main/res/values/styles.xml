<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/white</item>
    </style>
    <!--自定义dialog背景全透明无边框theme -->
    <style name="CustomDialog" parent="android:style/Theme.Dialog">
        <!--背景颜色及和透明程度-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="TabLayoutTextStyle16">
        <item name="android:textSize">@dimen/sp16</item>
        <item name="android:singleLine">true</item>
        <item name="tabPaddingStart">0dp</item>
        <item name="tabPaddingEnd">0dp</item>
    </style>
    <style name="MyCityPickerTheme" parent="DefaultCityPickerTheme">
        <item name="cpListItemTextSize">@dimen/dp16</item>
        <item name="cpListItemTextColor">@color/white</item>
        <item name="selectableItemBackground">@color/colorPrimaryNight</item>
        <item name="cpListItemHeight">50dp</item>
        <item name="cpGridItemBackground">@color/colorPrimaryNight</item>
        <item name="cpSearchHintTextColor">@color/colorCustomGray</item>
        <item name="cpSearchTextColor">@color/white</item>
        <item name="cpCancelTextColor">@color/white</item>
    </style>
</resources>